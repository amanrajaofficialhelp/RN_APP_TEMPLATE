import { RF } from "../utils/responsiveFont";

const TYPOGRAPHY = {
    h1: RF(28), // USED IN RARE CASE EVEN NOT FOR TITLE
    h2: RF(24),
    h3: RF(20),
    h4: RF(18),
    body: RF(16),
    caption: RF(12),
    description: RF(10),
};

export default TYPOGRAPHY;
